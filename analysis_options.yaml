include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Reference project we migrated the code from. It has its own pubspec and
    # is not part of this app's build.
    - somtalent/**
